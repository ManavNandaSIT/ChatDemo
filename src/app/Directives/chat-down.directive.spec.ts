import { ChatDownDirective } from './chat-down.directive';

describe('ChatDownDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatDownDirective();
    expect(directive).toBeTruthy();
  });
});
